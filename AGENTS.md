# 项目协作规范

## Git 提交与远程同步

- 每次改动完成后，创建对应的 Git commit。
- 将 commit 推送到 GitHub 远程仓库，便于后续追踪和回滚。

## 测试与验证

- 每次改动后，编写或更新相关测试。
- 交付给用户前，确保所有测试和验证均已通过。

## 项目文档约定

### 修改前

- 进行代码修改前，先阅读并参考 `docs/` 目录中的相关文档。
- 根据本次修改涉及的功能、模块或问题，定位并阅读对应的 `docs/` 文件。
- 如果文档中的规范、接口定义、架构说明或示例与现有代码不一致，先指出差异；未经明确确认，不要擅自偏离文档。

### 修改后

- 检查实现是否仍符合相关文档。
- 如果实现变更导致文档过期，同时更新对应文档。

### 缺少相关文档时

- 当 `docs/` 中没有与任务相关的说明时，明确说明这一点，再基于现有代码和项目惯例进行修改。
