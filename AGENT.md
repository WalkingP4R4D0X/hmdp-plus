注意事项：
每次改动完成后，都必须创建一个对应的Git commit，并推送到GitHub远程仓库，以便后续追踪和回滚
每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

# 项目文档约定

每次进行代码修改前，必须先阅读并参考 `docs/` 目录中的相关文档。

- 根据本次修改的功能、模块或问题，定位并阅读对应的 `docs/` 文件。
- 若文档中的规范、接口定义、架构说明或示例与现有代码不一致，先指出差异；未经明确确认，不要擅自偏离文档。
- 修改完成后，检查实现是否仍符合相关文档；如实现变更导致文档过期，同时更新对应文档。
- 当 `docs/` 中没有与任务相关的说明时，明确说明这一点，再基于现有代码和项目惯例进行修改。
